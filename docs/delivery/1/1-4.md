# [1-4] Linting & Formatting Configuration

[Back to task list](./tasks.md)

## Description

Configure ESLint and Prettier for the frontend (TypeScript/React) and golangci-lint for the backend (Go), establishing code quality standards from the start.

## Status History

| Timestamp | Event Type | From Status | To Status | Details | User |
|-----------|------------|-------------|-----------|---------|------|
| 2026-02-18 01:15:10 | Created | N/A | Proposed | Task file created | AI_Agent |
| 2026-02-18 01:36:25 | Status Change | Agreed | InProgress | Started implementation | AI_Agent |
| 2026-02-18 01:42:03 | Status Change | InProgress | Review | Implementation complete, internal AI review passed | AI_Agent |
| 2026-02-18 02:58:19 | Status Change | Review | Done | Task completed and verified | AI_Agent |

## Requirements

1. ESLint configured for the frontend with Next.js and TypeScript rules
2. Prettier configured for the frontend with consistent formatting rules
3. ESLint and Prettier integrated (no conflicting rules)
4. `golangci-lint` configuration file in `backend/` with sensible defaults
5. Lint commands runnable: `pnpm lint` (frontend), `golangci-lint run` (backend)
6. All existing code passes linting without errors

## Implementation Plan

1. Frontend: verify ESLint config from `create-next-app` scaffolding, extend if needed
2. Frontend: add Prettier with config file (`.prettierrc`), install `eslint-config-prettier`
3. Frontend: add `lint` and `format` scripts to `package.json`
4. Backend: create `.golangci.yml` with standard linters (govet, errcheck, staticcheck, gosimple, unused)
5. Verify all existing code passes both linters

## Test Plan

- `pnpm lint` in `frontend/` passes with no errors
- `pnpm format --check` in `frontend/` reports no formatting issues (or auto-fixes)
- `golangci-lint run ./...` in `backend/` passes with no errors

## Verification

- In `frontend/`, `pnpm lint` passed.
- In `frontend/`, `pnpm format:check` passed.
- In `backend/`, `golangci-lint run ./...` passed with `0 issues`.
- Internal AI review verdict: `pass`.
- Deferred findings: none.

## Files Modified

- `frontend/package.json`
- `frontend/eslint.config.mjs`
- `frontend/.prettierrc`
- `frontend/.prettierignore`
- `backend/.golangci.yml`
- `docs/delivery/1/1-4.md`
- `docs/delivery/1/tasks.md`
